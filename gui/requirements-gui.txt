# Archer uses system packages, not pip packages.
# Install on Arch:
#   sudo pacman -S python-gobject python-cairo python-dbus gtk4 libadwaita
#
# GTK >= 4.12 and libadwaita >= 1.6 are required.
# python-cairo renders telemetry charts and keyboard previews.
# GdkPixbuf (provided by the GTK runtime) renders the SVG tray icon.
# The system tray uses D-Bus StatusNotifierItem.
